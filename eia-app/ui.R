library(shiny)


fluidPage(

    # Application title
    titlePanel("Analysis on Data from the EIA"),

    # Sidebar with a slider input for number of bins
    sidebarLayout(
        sidebarPanel(
        
        ),

        # Show a plot of the generated distribution
        mainPanel(
          
        )
    )
)
